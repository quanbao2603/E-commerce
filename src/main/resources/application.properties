# ==============================
# APP / THYMELEAF
# ==============================
spring.application.name=WOMTech
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.cache=false

# ==============================
# MYSQL (DB: womtech)
# ==============================
spring.datasource.url=jdbc:mysql://localhost:3306/womtech?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=utf8&serverTimezone=Asia/Ho_Chi_Minh
spring.datasource.username=root
spring.datasource.password=your_password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate 6 (Spring Boot 3.5.x)
spring.jpa.hibernate.ddl-auto=update          # dev: update | prod: validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# ⛔ BỎ cấu hình dialect để Hibernate tự nhận — tránh cảnh báo MySQL8Dialect deprecated
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Ho_Chi_Minh

# ==============================
# LOGGING (khớp Hibernate 6)
# ==============================
logging.level.org.hibernate.SQL=debug
# Đổi logger binder cho Hibernate 6 (tránh cảnh báo BasicBinder cũ)
logging.level.org.hibernate.orm.jdbc.bind=trace

# ==============================
# MAIL (Gmail)
# ==============================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=nguyenquanbao2632005@gmail.com
spring.mail.password=gdnf qnid jyqz kgrk   # app password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.debug=false

app.mail.from=${spring.mail.username}
app.mail.fromName=WOMTech
